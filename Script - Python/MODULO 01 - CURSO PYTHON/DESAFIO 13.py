n1 = float(input('Qual é a altura? '))
n2 = float(input('E a largura? '))
a = n1 * n2
print('A sua área é {} m**2 e precisa {} litros de tinta para pintá-la'.format(a,a /2))
# Cada litro de tinta, pinta uma área de 2m**2
# Cada metro da parede precisa da tinta, do qual vai a consumindo, por isso que divide!